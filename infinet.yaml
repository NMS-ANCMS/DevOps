---
 - name: Third Party Software Installation
   hosts: infinetcluster # Infinet Inventory
   become: true
   tasks:
    - name: Ansible apt install Package
      apt:
        name: htop
        state: present
